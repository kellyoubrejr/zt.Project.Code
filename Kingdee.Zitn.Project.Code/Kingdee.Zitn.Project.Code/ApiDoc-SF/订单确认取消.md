# 速运API · 订单确认/取消接口

> **接口服务代码**: `EXP_RECE_UPDATE_ORDER`

---

## 1. 功能描述

接口用于以下场景：

1. 客户在确定将货物交付给顺丰托运后，将运单上的重要信息（如快件重量）通过此接口发送给顺丰
2. 客户在发货前取消订单
3. 子母件场景需要确认时需要传入所有的子母件单号

> **注意**：订单取消之后，订单号也是不能重复利用的。

---

## 2. 接口定义

### 2.1 公共参数

| 名称 | 值 |
|------|-----|
| 接口服务代码 | `EXP_RECE_UPDATE_ORDER` |
| 生产环境地址 | `https://bspgw.sf-express.com/std/service` |
| 香港生产环境 | `https://sfapi-hk.sf-express.com/std/service` |
| 沙箱环境地址 | `https://sfapi-sbox.sf-express.com/std/service` |
| 批量交易 | 不支持 |
| 接口类型 | 接入 |
| 报文类型 | JSON |

**开发规范要点**：

1. 通讯双方采用HTTP作为通讯协议
2. 提交方式为 **POST**，请求头须添加 `Content-type: application/x-www-form-urlencoded`，字符集编码统一使用 **UTF-8**
3. 参数需要通过 **http URL编码** 传送
4. 业务数据统一以字符串格式放在 `msgData` 字段中传送

---

### 2.2 公共请求参数

| 序号 | 参数列表 | 类型 | 是否必传 | 含义 |
|------|----------|------|----------|------|
| 1 | `partnerID` | String(64) | 是 | 合作伙伴编码/顾客编码 |
| 2 | `requestID` | String(40) | 是 | 请求唯一号UUID |
| 3 | `serviceCode` | String(50) | 是 | 接口服务代码（`EXP_RECE_UPDATE_ORDER`） |
| 4 | `timestamp` | long | 是 | 调用接口时间戳 |
| 5 | `msgDigest` | String(128) | 条件 | 数字签名（与accessToken二选一） |
| 6 | `accessToken` | String | 条件 | 访问令牌，OAuth2方式认证时必填 |
| 7 | `msgData` | String | 是 | 业务数据报文 |

---

### 2.3 请求参数 `<msgData>`

| # | 属性名 | 类型(约束) | 必填 | 默认值 | 描述 |
|---|--------|-----------|------|--------|------|
| 1 | `orderId` | String(64) | **是** | - | 客户订单号 |
| 2 | `dealType` | Number(1) | 否 | 1 | 操作标识：`1` 确认 / `2` 取消 |
| 3 | `waybillNoInfoList` | List | 条件 | - | 顺丰运单号（dealType=1时必填） |
| 4 | `customsBatchs` | String(20) | 否 | - | 报关批次 |
| 5 | `collectEmpCode` | String(30) | 否 | - | 揽收员工号 |
| 6 | `inProcessWaybillNo` | String(100) | 否 | - | 头程运单号 |
| 7 | `sourceZoneCode` | String(10) | 否 | - | 原寄地网点代码 |
| 8 | `destZoneCode` | String(10) | 否 | - | 目的地网点代码 |
| 9 | `totalWeight` | Number(17,5) | 否 | - | 货物总重量(kg)，精确到小数点后3位，值必须>0 |
| 10 | `totalVolume` | Number(16,5) | 否 | - | 货物总体积(cm³)，精确到小数点后3位 |
| 11 | `expressTypeId` | Number(5) | 否 | - | 快件产品类别 |
| 12 | `extraInfoList` | List | 否 | - | 扩展属性 |
| 13 | `totalLength` | Number(16,5) | 否 | - | 货物总长(cm) |
| 14 | `totalWidth` | Number(16,5) | 否 | - | 货物总宽(cm) |
| 15 | `totalHeight` | Number(16,5) | 否 | - | 货物总高(cm) |
| 16 | `serviceList` | List | 否 | - | 增值服务信息 |
| 17 | `isConfirmNew` | Number(1) | 否 | 0 | 是否走新通用确认：`1` 支持修改联系人 / `2` 支持改其他客户订单 |
| 18 | `destContactInfo` | OrderContactInfoDto | 否 | - | 收件人信息 |
| 19 | `isDocall` | Number(1) | 否 | - | 是否通知收派员上门：`1` 要求 |
| 20 | `specialDeliveryTypeCode` | String(3) | 否 | - | 特殊派送类型代码 |
| 21 | `specialDeliveryValue` | String(100) | 否 | - | 特殊派件具体表述 |
| 22 | `sendStartTm` | Date | 否 | - | 预约时间（上门揽收时间） |
| 23 | `pickupAppointEndtime` | Date | 否 | - | 上门揽收截止时间 |
| 24 | `remark` | String(100) | 否 | - | 备注 |

> 丰桥下订单接口默认自动确认，不需客户重复确认；如需手工确认请前往控制台→查看API→下单接口→修改为不自动确认

---

#### 2.3.1 waybillNoInfoList 元素

| # | 属性名 | 类型(约束) | 必填 | 默认值 | 描述 |
|---|--------|-----------|------|--------|------|
| 1 | `waybillType` | Number(1) | 否 | - | 运单号类型：`1` 母单 / `2` 子单 / `3` 签回单 |
| 2 | `waybillNo` | String(15) | 否 | - | 运单号 |
| 3 | `boxNo` | String(64) | 否 | - | 箱号（月结维度唯一） |
| 4 | `length` | Number(16,3) | 否 | - | 长(cm) |
| 5 | `width` | Number(16,3) | 否 | - | 宽(cm) |
| 6 | `height` | Number(16,2) | 否 | - | 高(cm) |
| 7 | `weight` | Number(16,2) | 否 | - | 重量(kg) |

---

#### 2.3.2 extraInfoList 元素

| # | 属性名 | 类型(约束) | 必填 | 默认值 | 描述 |
|---|--------|-----------|------|--------|------|
| 1 | `attrName` | String(256) | 否 | - | 扩展字段名 |
| 2 | `attrVal` | String(1024) | 否 | - | 扩展字段值 |

**扩展字段备注**：

| attrName | attrVal |
|----------|---------|
| `userId` | 商家或合作店铺id（丰网业务） |
| `branchCode` | 丰网合作网点（丰网必填） |
| `branchAddressId` | 丰网合作地址id |
| `channelCode` | 渠道编码 |
| `subsidySource` | 国补标识：`dy`抖音 `sn`苏宁 `tb`淘天 `pdd`拼多多 `ks`快手 `wph`唯品会 `dw`得物 `xhs`小红书 `sph`视频号 `merchant`商家自营 `cancel`取消 |
| `infoCollectSource` | 采集服务标识（取值同subsidySource） |
| `wpMerchantCode` | 微派商户编码（测试可用：`B2019020002-0014`） |
| `wpServiceCode` | 微派任务编码（测试：`Pro24125839`手机 / `Pro24125847`平板 / `Pro24125848`电脑 / `Pro24125849`智能穿戴） |
| `wpExtJson` | `{"sn":"123456","imei":"123456"}` |

---

#### 2.3.3 serviceList 元素

| # | 属性名 | 类型(约束) | 必填 | 默认值 | 描述 |
|---|--------|-----------|------|--------|------|
| 1 | `name` | String(20) | **是** | - | 增值服务名（如COD等） |
| 2 | `value` | String(30) | 条件 | - | 增值服务扩展属性 |
| 3 | `value1` | String(30) | 条件 | - | 增值服务扩展属性1 |
| 4 | `value2` | String(30) | 条件 | - | 增值服务扩展属性2 |
| 5 | `value3` | String(30) | 条件 | - | 增值服务扩展属性3 |
| 6 | `value4` | String(30) | 条件 | - | 增值服务扩展属性4 |

---

#### 2.3.4 OrderContactInfoDto（收件人信息）

| # | 属性名 | 类型(约束) | 必填 | 描述 |
|---|--------|-----------|------|------|
| 1 | `company` | String(100) | 否 | 公司名称 |
| 2 | `contact` | String(100) | **是** | 联系人 |
| 3 | `tel` | String(20) | 条件 | 收方电话 |
| 4 | `mobile` | String(20) | 条件 | 收方手机 |
| 5 | `country` | String(30) | **是** | 国家或地区2位代码 |
| 6 | `province` | String(30) | **是** | 省级行政区名称 |
| 7 | `city` | String(100) | **是** | 地级行政区名称 |
| 8 | `county` | String(30) | **是** | 县/区级行政区名称 |
| 9 | `address` | String(200) | **是** | 详细地址 |

---

### 2.4 公共响应参数

| # | 属性名 | 类型(约束) | 必填 | 默认值 | 描述 |
|---|--------|-----------|------|--------|------|
| 1 | `success` | String | 是 | - | `true` 请求成功 / `false` 请求失败 |
| 2 | `errorCode` | String | 是 | - | 错误编码（`S0000` 成功） |
| 3 | `errorMsg` | String | 是 | - | 错误描述 |
| 4 | `msgData` | String | 是 | - | 返回的详细数据 |

---

### 2.5 响应参数 `<msgData>`

| # | 属性名 | 类型(约束) | 必填 | 描述 |
|---|--------|-----------|------|------|
| 1 | `orderId` | String(64) | 是 | 客户订单号 |
| 2 | `waybillNoInfoList` | List | 否 | 顺丰运单号列表 |
| 3 | `resStatus` | Number(1) | 是 | 结果：`1` 订单号与运单不匹配 / `2` 操作成功 |
| 4 | `extraInfoList` | List | 否 | 扩展属性 |

---

### 2.6 请求示例 (JSON)

**订单确认**：

```json
{
    "dealType": 1,
    "orderId": "BZL51054473992769999",
    "totalHeight": 29.98,
    "totalLength": 29.98,
    "totalVolume": 26946.035992000005,
    "totalWeight": 2.09,
    "totalWidth": 29.98,
    "waybillNoInfoList": [
        {
            "waybillNo": "SF2000090670189",
            "waybillType": 1
        }
    ]
}
```

**订单取消**：

```json
{
    "dealType": 2,
    "language": "zh-CN",
    "orderId": "eb21c793-a45a-4d1e-9a2e-1b6e0cd49668",
    "totalWeight": 1,
    "waybillNoInfoList": []
}
```

---

### 2.7 返回示例 (JSON)

**订单确认成功**：

```json
{
    "apiResponseID": "000173271983983FEDCAD1803FA64A3F",
    "apiErrorMsg": "",
    "apiResultCode": "A1000",
    "apiResultData": "{\"success\":true,\"errorCode\":\"S0000\",\"errorMsg\":null,\"msgData\":{\"orderId\":\"5ed9696e-a81d-4a5b-968d-182c2d8c09e0\",\"waybillNoInfoList\":[{\"waybillType\":1,\"waybillNo\":\"SF7444400048449\"}],\"resStatus\":2,\"extraInfoList\":null}}"
}
```

**订单确认失败**：

```json
{
    "apiResponseID": "000173271968963FA47C03E68000103F",
    "apiErrorMsg": "",
    "apiResultCode": "A1000",
    "apiResultData": "{\"success\":false,\"errorCode\":\"8024\",\"errorMsg\":\"未下单\",\"msgData\":null}"
}
```

**订单取消成功**：

```json
{
    "success": true,
    "errorCode": "S0000",
    "errorMsg": null,
    "msgData": {
        "orderId": "eb21c793-a45a-4d1e-9a2e-1b6e0cd49668",
        "waybillNoInfoList": [{
            "waybillType": 1,
            "waybillNo": "SF7444400043064"
        }],
        "resStatus": 2,
        "extraInfoList": null
    }
}
```

**订单取消失败**：

```json
{
    "apiErrorMsg": "",
    "apiResponseID": "00019AC8457F7D3FC372D7B021E1603F",
    "apiResultCode": "A1000",
    "apiResultData": "{\"success\":false,\"errorCode\":\"6118\",\"errorMsg\":\"订单号不能为空\",\"msgData\":null}"
}
```

---

## 3. 错误代码

### 3.1 平台结果码（API层）

| 标识 | 说明 | 处理建议 |
|------|------|----------|
| **A1000** | 统一接入平台校验成功 | 表示接口调用正常（不代表业务处理成功） |
| **A1001** | 必传参数不可为空 | serviceCode/partnerID/requestID/timestamp/msgDigest/msgData不可为空 |
| **A1002** | 请求时效已过期 | 参考OAuth2时效 |
| **A1003** | IP无效 | 参考常见问题 |
| **A1004** | 无对应服务权限 | 联系销售经理配置权限 |
| **A1005** | 流量受控 | 测试环境流量限制5000，请勿压测 |
| **A1006** | 数字签名无效 | 参考签名加解密常见问题 |
| **A1007** | 重复请求 | 过一分钟再尝试 |
| **A1008** | 数据解密失败 | - |
| **A1009** | 目标服务异常或不可达 | - |
| **A1099** | 系统异常 | - |

---

### 3.2 业务异常代码

| # | errorCode | 描述 | 处理建议 |
|---|-----------|------|----------|
| 1 | **20052** | 月结卡号不匹配 不允许操作该订单 | 修改月结卡号匹配后确认 |
| 2 | **8019** | 订单已确认或已消单 | 确认下单模板是否配置为下单自动确认 |
| 3 | **8018** | 未获取到订单信息 | 修改orderId |
| 4 | **6136** | 未传入订单确认信息 | 传入报文格式有问题 |
| 5 | **8037** | 已消单 | 订单已取消，再次取消会报错 |
| 6 | **8017** | 订单号与运单号不匹配 | 检查orderId跟运单号是否匹配 |
| 7 | **8228** | 传入增值服务不能通过确认接口修改 | 对应增值服务不支持确认接口修改 |
| 8 | **8253** | 订单已取消 | 订单已取消，再次取消会报错 |
| 9 | **8252** | 订单已确认 | 订单已确认，再次确认会报错 |
| 10 | **20034** | 预约时间必须大于当前时间 | sendStartTm要大于当前时间 |

---

## 国补传参示例

```json
"extraInfoList": [
    {
        "attrName": "subsidySource",
        "attrVal": "sn"
    },
    {
        "attrName": "wpMerchantCode",
        "attrVal": "B2019020002-0014"
    },
    {
        "attrName": "wpServiceCode",
        "attrVal": "Pro24123096"
    },
    {
        "attrName": "wpExtJson",
        "attrVal": "{\"imei\": \"8621111111192\",\"sn\": \"2MH11111112368\"}"
    }
]
```

> **说明**：
> 1. 国补传值要求sn/imei是否传入参考业务场景定义，原则上3C品类国补必传
> 2. 沙箱环境直接传入字段即可触发，可以使用示例报文请求验证
